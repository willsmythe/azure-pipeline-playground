
jobs:
- job: linux
  strategy:
    matrix:
      LinuxNode15:
        NODE_VERSION: 15.x
        VM_IMAGE: 'ubuntu-16.04'            
      LinuxNode10:
        NODE_VERSION: 10.x
        VM_IMAGE: 'ubuntu-16.04'
  steps:
    - task: NodeTool@0
      inputs:
        versionSpec: $(NODE_VERSION)
    - script: |
        echo $(NODE_VERSION)
