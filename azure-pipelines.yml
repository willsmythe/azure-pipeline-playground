stages:
- stage: build
  jobs:
  - job: publish
    pool:
      name: Default
    steps:
    - script: |
        md a1
        copy *.yml a1
      displayName: Copy files2

    - task: PublishPipelineArtifact@0
      inputs:
        artifactName: a1
        targetPath: '$(Build.SourcesDirectory)/a1'

- stage: deploy
  jobs:
  - deployment: deploy
    pool:
      name: Default
    strategy:
      runOnce:
        deploy:
        steps:
        - script: echo 'Hello'
