steps:
- bash: |
    git config --global user.name "VssAdmin"
    git config --global user.email "VssAdmin@example.org"
    git status
    git remote -v
    cp azure-pipelines.yml test.yml
    git checkout -b t
    git add .
    git commit -m "Commit test.yml"
    git push origin t

- bash: |
    curl $(System.TeamFoundationCollectionUri)/_apis/git/pullrequests -H "Authorization: Bearer ${SYSTEM_ACCESSTOKEN}"
  env:
    SYSTEM_ACCESSTOKEN: $(System.AccessToken)
  condition: always()
